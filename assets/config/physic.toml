# max_rockets = 1024
# particles_per_explosion = 256
# particles_per_trail = 64
# rocket_interval_mean = 1.0
# rocket_interval_variation = 0.75
# rocket_max_next_interval = 0.5
#
max_rockets = 64
particles_per_explosion = 768
particles_per_trail = 64
rocket_interval_mean = 0.025
rocket_interval_variation = 0.01875
rocket_max_next_interval = 0.025
explosion_threshold = 50.0
#
# max_rockets = 8192
# particles_per_explosion = 256
# particles_per_trail = 64
# rocket_interval_mean = 0.0010
# rocket_interval_variation = 0.0075
# rocket_max_next_interval = 0.001

# default values
spawn_rocket_margin = 250.0
spawn_rocket_vertical_angle = 1.570796327
spawn_rocket_angle_variation = 0.3
spawn_rocket_min_speed = 350.0
spawn_rocket_max_speed = 500.0

gravity = -200.0
initial_rocket_speed = 100.0
explosion_min_vel = 60.0
explosion_max_vel = 200.0

# Temps d'anticipation de l'audio en ms pour le lancement (par défaut 25.0)
audio_launch_anticipation_ms = 25.0

# Temps d'anticipation de l'audio en ms pour l'explosion (par défaut 25.0)
audio_explosion_anticipation_ms = 25.0
